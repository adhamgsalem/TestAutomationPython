[pytest]
# This section configures pytest's behavior

# Specify the directories where pytest should search for test files
testpaths = 
    tests

# Add any markers you use in your tests
markers =
    smoke: A quick smoke test
    regression: A test for regression testing

# Define command-line options
addopts = --maxfail=5 --disable-warnings
